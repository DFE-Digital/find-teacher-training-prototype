<div id="apply" class="govuk-!-margin-bottom-8">

<h2 class="govuk-heading-l">Apply</h2>

  {% if not data.nearingEndOfCycle %}

    {% if course.has_vacancies %}

      <p class="govuk-body">Applications opened in October 2022.</p>

      <p class="govuk-body">Teacher training providers allocate places as people apply through the year, and courses stay open until they are full.</p>

      <p class="govuk-body">If you’re keen to join a popular course it’s wise to apply as soon as you’re ready rather than putting it off.</p>

      <p class="govuk-body">You can apply to 4 providers at a time. You’ll need to create an account to start your application.</p>

      {{ govukButton({
        text: "Apply for this course",
        href: "#",
        isStartButton: true
      }) }}

    {% else %}
      {{ govukWarningText({
        text: "You cannot apply for this course because it currently has no vacancies. To find courses with vacancies, change your search settings to ‘Only show courses with vacancies’.",
        iconFallbackText: "Warning"
      }) }}
    {% endif %}

  {% else %}


    <p class="govuk-body">
      Courses are currently closed but you can still <a class="govuk-link" href="#">start or continue an application</a> anyway.
    </p>

    <p class="govuk-body">You’ll be able to:</p>

    <ul class="govuk-list govuk-list--bullet">
      <li>
        find courses starting in the NEXT_CYCLE_YEAR_RANGE academic year from FIND_REOPENS
      </li>
      <li>submit your application from APPLY_REOPENS</li>
    </ul>

  {% endif %}
</div>
