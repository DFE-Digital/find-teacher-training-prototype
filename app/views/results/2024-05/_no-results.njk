<p class="govuk-body">

  {%- if keywords -%}

    {%- if hasFilters -%}

      {%- if selectedFilters.categories.length > 1 or selectedFilters.categories[0].items.length > 1 -%}

        There are no results for ‘{{ keywords }}’ and the selected filters.

      {%- else -%}

        There are no results for ‘{{ keywords }}’ and the selected filter.

      {%- endif -%}

    {%- else -%}

      There are no results for ‘{{ keywords }}’.

    {%- endif -%}

  {%- else -%}

    {%- if hasFilters -%}

      {%- if selectedFilters.categories.length > 1 or selectedFilters.categories[0].items.length > 1 -%}

        There are no results for the selected filters.

      {%- else -%}

        There are no results for the selected filter.

      {%- endif -%}

    {%- else -%}

      There are no results

    {%- endif -%}

  {%- endif -%}

</p>
